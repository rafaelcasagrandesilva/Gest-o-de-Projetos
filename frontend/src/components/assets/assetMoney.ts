/** @deprecated Importe de `@/utils/currency` — reexport de compatibilidade. */
export {
  parseCurrencyInput as parseBRLInput,
  formatCurrencyField as formatMoneyFieldBr,
  formatCurrencyInputFromApi as moneyFieldFromNumber,
  sanitizeCurrencyTyping,
  sanitizeCurrencyTyping as sanitizeMoneyTyping,
  formatCurrency,
} from "@/utils/currency";
