# ENV
.env
.env.*
# Template versionado (exceção ao .env.*)
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyo

# Virtualenv
.venv/
venv/

# Logs
*.log

# Build
dist/
build/

# Frontend
node_modules/
*.tsbuildinfo

# Uploads
var/
uploads/

# Mac
.DS_Store

# IDE
.vscode/
.idea/

# --- legado / extras do projeto ---
*.pyd
*.db
*.sqlite3
backup_sgp.sql
*.sql
*.dump

# Dumps/backups do banco (NUNCA versionar dados de produção)
backups/
*.backup

# Dados pessoais reais NUNCA são versionados.
# `legal_seed.json` é gerado LOCALMENTE por scripts/build_legal_seed.py a partir da planilha
# oficial e carrega CPF, nome, rescisão e FGTS de ex-colaboradores. Em produção a carga é feita
# pela importação da planilha na tela (Jurídico → Administração → Importações); este arquivo
# serve apenas para levantar um ambiente de desenvolvimento.
app/scripts/data/
